# AGENTS.md — AI 协作规则（必读）

> 本文件是本项目对 **任何 AI 代理** 的强制性协作规范。开始修改项目前必须完整阅读；违反本文件规则的变更应被拒绝或回退。

## 1. AI 交接制度（必须遵守）

### 1.1 修改项目前，必须依次阅读

1. `AGENTS.md`（本文件）
2. `MASTER_PLAN.md`（总体技术方案）
3. `PROJECT_STATE.md`（当前阶段与状态）
4. `DECISIONS.md`（关键决策记录）
5. `TASKS.md`（任务清单与验收）
6. `HANDOFF.md`（上一轮交接说明）

### 1.2 每轮工作结束，必须更新

- `PROJECT_STATE.md`：当前阶段、已完成、进行中、下一步、已验证命令。
- `TASKS.md`：任务状态与验收结果。
- `CHANGELOG.md`：实际变更记录。
- `HANDOFF.md`：修改文件、运行命令、测试结果、已知问题、环境要求、下一位 AI 的明确起点。

### 1.3 通用纪律

- 不删除、不覆盖用户已有内容；文件已存在时先读取再增量修改。
- 不擅自安装依赖；本仓库在 Phase 0 明确不安装任何依赖。
- 不自动 git commit；需要提交时先向用户确认。
- 不修改 Git 全局配置。
- 所有文档与代码使用 **UTF-8 编码**。

## 2. 数据准确性铁律（最高优先级）

> 详细规范见 `docs/data-sources/data-quality.md`，以下为必须内化的硬性规则：

1. **禁止把搜索引擎摘要当作精确实时价或收盘价。**
2. 每条行情记录必须包含：`symbol`、`market`、`trade_date`、`quote_time`、`fetched_at`、`source`、`price_type`、`adjustment`、`is_delayed`、`raw_value`、`normalized_value`。
3. 关键收盘数据尽量进行**双源校验**；来源冲突时标记异常，**不输出确定性结论**。
4. 用户通过通达信或券商终端人工确认的价格记录标记为 `manual_verified`。
5. 数据缺失时**明确显示缺失**，不能用历史缓存伪装成当天数据。
6. 所有行情页面显示**数据来源和截至时间**。
7. 后续为数据适配器编写**延迟、空值、字段变化和来源冲突**测试（tests/data/）。

## 3. 确定性计算边界

- 数值计算、成本计算、交易规则（如 T+1）必须使用**确定性代码**。
- 大模型（DeepSeek / OpenAI / Ollama）只做辅助解读，**输出不得进入任何计算链路**。
- 做T辅助中"负成本"仅表示累计已实现收益超过剩余持仓账面成本，**仍然存在市场风险**；任何输出都必须保留此风险语义。

## 4. 隐私与安全

- 以下路径与文件**禁止**出现在 git 跟踪中（.gitignore 已覆盖，新增模式需先确认）：
  - `data/private/`、`storage/db/`、`storage/cache/`、`storage/logs/`、`.env`、券商导出原件。
- 不得索要、读取或保存**券商密码、短信验证码、交易密钥**。
- 真实持仓数据只允许写入 `data/private/`，不得写入任何可提交文件。
- 示例与模板（如 `data/imports/positions.example.csv`）只允许包含**虚拟示例**。

## 5. 变更纪律

- 每个 Phase 开始前，先更新 `MASTER_PLAN.md` 与 `PROJECT_STATE.md` 中的规划，再写代码。
- 实现任何数据源适配器时，同步交付 `tests/data/` 下的质量测试（延迟、空值、字段变化、来源冲突）。
- 工具只提供数据、复盘、预警和计划：不自动登录券商、不保存券商密码、不自动下单。
